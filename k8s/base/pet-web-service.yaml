apiVersion: v1
kind: Service
metadata:
  name: pet-web-service
  namespace: pet
  labels:
    app: web
    app: apache
spec:
  selector:
    app: web
    name: apache
  ports:
    - port: 80
      targetPort: 80
  type: NodePort